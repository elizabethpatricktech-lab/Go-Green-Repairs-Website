export interface ProfileData {
  first_name: string;
  last_name: string;
  email: string;
  is_verified: boolean;

  phone: string;
  address: string;
  city: string;
  state: string;
  zip_code: string;
}
